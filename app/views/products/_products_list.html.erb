<div class="container">
  <div class="d-flex flex-column gap-3">
    <%= paginate products %>
    <div class="row">
      <% products.each do |product| %>
        <div class="col-3">
          <div class="card text-center p-2 mb-3">
            <div class="d-block mx-auto" style="width: 64px;">
              <%= image_tag product.image.variant(:page_display), size: '64x64', class: 'card-img-top' if product.image.attached? %>
            </div>
            <div class="card-body">
              <h5 class="card-title"><%= product.name %></h5>
              <p class="card-text">
                <%= number_with_delimiter(product.price) %>円（税抜）
              </p>
              <p class="card-text">
                <%= product.description %>
              </p>
              <%= link_to '詳細', product_path(product), class: 'btn btn-primary btn-sm' %>
            </div>
          </div>
        </div>
      <% end %>
    </div>
    <%= paginate products %>
  </div>
</div>
